"""
colibri.forward_map.py

This module implements the forward map, i.e. the map from the PDF grid to the theory predictions for each dataset, using the FK tables.

"""


def forward_map(make_pred_data):
    """
    Internal alias function for make_pred_data.
    """
    return make_pred_data
